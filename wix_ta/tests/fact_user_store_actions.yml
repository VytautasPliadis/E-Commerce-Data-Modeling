version: 2

models:
  - name: fact_user_store_actions
    columns:
      - name: user_id
        tests:
          - relationships:
              to: ref('user_dim')
              field: user_id
      - name: store_id
        tests:
          - relationships:
              to: ref('store_dim')
              field: store_id